var c : Int;
var d : Int;
func add(a : Int, b : Int) -> Int {
    return a + b;
}

var e : Int;

func main() {
    var a : Int = 12;
    var b : Float = 1.2 + 3.6;
    if (b > 1.2) {
        
    }

    while (a > 0) {
        if (add(a, sx1) / 2 == 1) {
            
            break;
        }
        a = a - 1;
    }
    return 0;
}

var f : Int