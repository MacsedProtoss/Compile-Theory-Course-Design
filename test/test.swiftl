var c : Int = 2 + 3 * 4 + 5;
var d : Int;
func add(a : Int, b : Int) -> Int {
    return a + b * 2 + 3;
}

var e : Int;

func main() {
    var a : Int = 12;
    var b : Float = 1.2 + 3.6;
    a++
    if (!a) {
        
    }else{
        
    }

    while (a > 0) {
        a = a - 1;
        if (add(a, 1) / 2 == 1) {
            var t : Int = 0
            while (t < 5){
                printI(t)
            }
            break;
        }else{
            continue;
        }
        break;
    }
    return ;
}

var f : Int