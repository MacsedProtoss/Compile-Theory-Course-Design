var c : Int = 2 + 3 * 4 + 5;
var d : Int;
func add(a : Int, b : Int) -> Int {
    return a + b * 2 + 3;
}

var e : Int;

func main() {
    var a : Int = 12;
    var b : Float = 1.2 + 3.6;
    a++;
    1++;
    if (!a) {
        
    }else{

    }

    while (a > 0) {
        if (add(a, 1) / 2 == 1) {
            
            break;
        }
        a = a - 1;
    }
    return 0;
}

var f : Int