
func add(Int a, Int b) -> Int {
    return a + b;
}

func main() {
    var a : Int = 12;
    var b : Float = 1.2 + 3.6;
    if (b > 1.2) {
        
    }

    while (a > 0) {
        if (add(a, 1) / 2 == 1) {
            
            break;
        }
        a = a - 1;
    }
    return 0;
}